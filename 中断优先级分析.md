# STM32G4 中断优先级分组分析与设计原则

## 当前配置分析

### 1. 优先级分组设置
- **当前分组**: `NVIC_PRIORITYGROUP_4`
- **抢占优先级位数**: 4位（0-15，共16级）
- **子优先级位数**: 0位（无效）

### 2. 当前中断优先级配置

所有中断都设置为 `HAL_NVIC_SetPriority(IRQn, 0, 0)`，即：
- **抢占优先级**: 0
- **子优先级**: 0（在GROUP_4下无效）

#### 中断列表：
- **DMA中断** (所有通道): 优先级 0
- **UART中断** (UART1/2/3/4/5): 优先级 0
- **TIM1中断**: 优先级 0
- **HRTIM中断**: 优先级 0

## ⚠️ 当前配置的问题

### 问题1: 所有中断优先级相同
所有中断的抢占优先级都是0，这意味着：
- **无法实现中断嵌套**：低优先级中断无法被高优先级中断抢占
- **响应时间不可预测**：如果DMA中断正在执行，HRTIM中断必须等待
- **实时性差**：关键实时任务（如HRTIM、定时器）可能被阻塞

### 问题2: 可能导致的问题
1. **DMA传输阻塞关键中断**：如果DMA中断正在处理，HRTIM中断无法立即响应
2. **UART中断阻塞定时器**：UART接收中断可能阻塞TIM1定时器中断
3. **中断响应延迟**：所有中断按FIFO顺序处理，无法保证关键任务的实时性

## 中断优先级分组设计原则

### 1. 优先级分组选择原则

STM32G4支持5种优先级分组（NVIC_PRIORITYGROUP_0 到 NVIC_PRIORITYGROUP_4）：

| 分组 | 抢占优先级位数 | 子优先级位数 | 抢占优先级范围 | 子优先级范围 | 适用场景 |
|------|---------------|-------------|---------------|-------------|---------|
| GROUP_0 | 0位 | 4位 | 无 | 0-15 | 不使用抢占，仅顺序执行 |
| GROUP_1 | 1位 | 3位 | 0-1 | 0-7 | 简单两级优先级 |
| GROUP_2 | 2位 | 2位 | 0-3 | 0-3 | 中等复杂度系统 |
| GROUP_3 | 3位 | 1位 | 0-7 | 0-1 | 复杂系统，需要多级抢占 |
| GROUP_4 | 4位 | 0位 | 0-15 | 无 | 最大抢占灵活性，无子优先级 |

### 2. 设计原则

#### 原则1: 关键实时任务优先级最高
- **HRTIM中断**：高精度定时器，用于PWM生成，必须最高优先级
- **系统定时器（SysTick）**：系统心跳，应设置较高优先级
- **关键定时器中断**：如TIM1用于编码器触发，需要较高优先级

#### 原则2: 数据采集任务中等优先级
- **ADC中断**：数据采集，需要及时响应但不需要最高优先级
- **DMA传输完成中断**：数据传输完成通知，中等优先级

#### 原则3: 通信任务较低优先级
- **UART接收中断**：数据接收，可以容忍一定延迟
- **UART发送完成中断**：发送完成通知，优先级可以较低

#### 原则4: 避免优先级反转
- 不要将低优先级任务设置为高优先级
- 确保高优先级任务执行时间短，避免长时间阻塞低优先级任务

### 3. 推荐的优先级分配（GROUP_4）

使用 `NVIC_PRIORITYGROUP_4` 时，抢占优先级分配建议：

| 中断类型 | 抢占优先级 | 说明 |
|---------|-----------|------|
| HRTIM中断 | 0 | 最高优先级，PWM生成 |
| SysTick | 1 | 系统定时器 |
| TIM1中断 | 2 | 编码器触发定时器 |
| ADC中断 | 3-4 | 数据采集 |
| DMA传输完成 | 5-6 | DMA传输完成通知 |
| UART接收 | 7-8 | 串口数据接收 |
| UART发送完成 | 9-10 | 串口发送完成 |

### 4. 推荐的优先级分配（GROUP_2）

如果使用 `NVIC_PRIORITYGROUP_2`（2位抢占，2位子优先级）：

| 中断类型 | 抢占优先级 | 子优先级 | 说明 |
|---------|-----------|---------|------|
| HRTIM中断 | 0 | 0 | 最高优先级 |
| SysTick | 0 | 1 | 系统定时器 |
| TIM1中断 | 1 | 0 | 编码器触发 |
| ADC中断 | 1 | 1-2 | 数据采集 |
| DMA传输完成 | 2 | 0-1 | DMA通知 |
| UART接收 | 2 | 2-3 | 串口接收 |
| UART发送完成 | 3 | 0-3 | 串口发送 |

## 建议的配置修改

### 方案1: 保持GROUP_4，调整优先级

```c
// 在 HAL_Init() 中已设置：HAL_NVIC_SetPriorityGrouping(NVIC_PRIORITYGROUP_4);

// HRTIM中断 - 最高优先级
HAL_NVIC_SetPriority(HRTIM1_Master_IRQn, 0, 0);
HAL_NVIC_SetPriority(HRTIM1_TIMB_IRQn, 0, 0);

// TIM1中断 - 高优先级
HAL_NVIC_SetPriority(TIM1_UP_TIM16_IRQn, 1, 0);

// DMA中断 - 中等优先级
HAL_NVIC_SetPriority(DMA1_Channel1_IRQn, 2, 0);  // ADC1
HAL_NVIC_SetPriority(DMA1_Channel2_IRQn, 2, 0);  // ADC2
HAL_NVIC_SetPriority(DMA2_Channel2_IRQn, 3, 0);   // UART3_TX
HAL_NVIC_SetPriority(DMA2_Channel3_IRQn, 3, 0);  // UART5_TX
HAL_NVIC_SetPriority(DMA2_Channel4_IRQn, 3, 0);  // UART4_TX
HAL_NVIC_SetPriority(DMA1_Channel5_IRQn, 4, 0);  // UART3_RX
HAL_NVIC_SetPriority(DMA1_Channel6_IRQn, 4, 0);  // UART4_RX
HAL_NVIC_SetPriority(DMA1_Channel7_IRQn, 4, 0);  // UART5_RX

// UART中断 - 较低优先级
HAL_NVIC_SetPriority(USART1_IRQn, 5, 0);
HAL_NVIC_SetPriority(USART2_IRQn, 5, 0);
HAL_NVIC_SetPriority(USART3_IRQn, 5, 0);
HAL_NVIC_SetPriority(UART4_IRQn, 5, 0);
HAL_NVIC_SetPriority(UART5_IRQn, 5, 0);
```

### 方案2: 使用GROUP_2，提供更多灵活性

```c
// 修改 HAL_Init() 中的设置
HAL_NVIC_SetPriorityGrouping(NVIC_PRIORITYGROUP_2);

// HRTIM中断 - 最高优先级
HAL_NVIC_SetPriority(HRTIM1_Master_IRQn, 0, 0);
HAL_NVIC_SetPriority(HRTIM1_TIMB_IRQn, 0, 1);

// TIM1中断 - 高优先级
HAL_NVIC_SetPriority(TIM1_UP_TIM16_IRQn, 1, 0);

// DMA中断 - 中等优先级
HAL_NVIC_SetPriority(DMA1_Channel1_IRQn, 2, 0);  // ADC1
HAL_NVIC_SetPriority(DMA1_Channel2_IRQn, 2, 1);  // ADC2
HAL_NVIC_SetPriority(DMA2_Channel2_IRQn, 2, 2);  // UART3_TX
HAL_NVIC_SetPriority(DMA2_Channel3_IRQn, 2, 2);  // UART5_TX
HAL_NVIC_SetPriority(DMA2_Channel4_IRQn, 2, 2);  // UART4_TX

// UART中断 - 较低优先级
HAL_NVIC_SetPriority(USART1_IRQn, 3, 0);
HAL_NVIC_SetPriority(USART2_IRQn, 3, 1);
HAL_NVIC_SetPriority(USART3_IRQn, 3, 2);
HAL_NVIC_SetPriority(UART4_IRQn, 3, 2);
HAL_NVIC_SetPriority(UART5_IRQn, 3, 3);
```

## 总结

### 当前配置评估
- ❌ **不合理**：所有中断优先级相同，无法实现中断嵌套
- ❌ **实时性差**：关键任务可能被阻塞
- ❌ **响应时间不可预测**：中断按FIFO顺序处理

### 建议
1. **保持GROUP_4**，但调整各中断的抢占优先级
2. **关键实时任务**（HRTIM、TIM1）设置为最高优先级（0-1）
3. **数据采集任务**（ADC、DMA）设置为中等优先级（2-4）
4. **通信任务**（UART）设置为较低优先级（5+）

这样可以确保：
- ✅ 关键实时任务不会被阻塞
- ✅ 中断响应时间可预测
- ✅ 系统实时性得到保证
