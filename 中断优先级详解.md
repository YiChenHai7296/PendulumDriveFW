# STM32 中断优先级详解：抢占优先级 vs 子优先级

## 核心概念

### 1. 抢占优先级（Preemption Priority）
**作用：决定中断是否可以打断正在执行的中断**

- **高抢占优先级可以打断低抢占优先级的中断**
- 就像医院的急诊科：急诊病人（高优先级）可以打断普通病人（低优先级）的诊疗
- **数值越小，优先级越高**（0 是最高的）

### 2. 子优先级（Sub Priority）
**作用：当多个中断同时发生且抢占优先级相同时，决定谁先执行**

- **子优先级不能用于中断嵌套**，只能决定排队顺序
- 就像银行排队：如果都是VIP客户（抢占优先级相同），子优先级决定谁先办理
- **数值越小，优先级越高**（0 是最高的）

## 实际例子

### 场景1：不同抢占优先级（可以嵌套）

假设配置：
- HRTIM中断：抢占优先级 = 0
- UART中断：抢占优先级 = 5

**执行流程：**
```
时间轴：
T1: UART中断开始执行（抢占优先级5）
T2: HRTIM中断发生（抢占优先级0）
T3: HRTIM中断立即打断UART中断，开始执行
T4: HRTIM中断执行完毕
T5: 返回继续执行UART中断
```

**结果：** HRTIM中断可以打断UART中断，因为它的抢占优先级更高。

### 场景2：相同抢占优先级，不同子优先级（不能嵌套）

假设配置（GROUP_2，2位抢占，2位子优先级）：
- DMA1中断：抢占优先级 = 2，子优先级 = 0
- DMA2中断：抢占优先级 = 2，子优先级 = 1
- DMA3中断：抢占优先级 = 2，子优先级 = 2

**执行流程：**
```
时间轴：
T1: DMA1中断开始执行（抢占2，子0）
T2: DMA2中断发生（抢占2，子1）
T3: DMA2中断等待，不能打断DMA1（抢占优先级相同）
T4: DMA3中断发生（抢占2，子2）
T5: DMA3中断等待，排在DMA2后面
T6: DMA1中断执行完毕
T7: DMA2中断开始执行（子优先级更高）
T8: DMA2中断执行完毕
T9: DMA3中断开始执行
```

**结果：** 因为抢占优先级相同，它们不能相互打断，只能按子优先级排队执行。

## 优先级分组的影响

### GROUP_4（当前配置）
- 抢占优先级：4位（0-15，共16级）
- 子优先级：0位（无效）

**特点：**
- ✅ 最多16个不同的抢占优先级级别
- ✅ 可以实现精细的中断嵌套控制
- ❌ 没有子优先级，相同抢占优先级的中断按硬件优先级（IRQ号）排队

**示例：**
```c
HAL_NVIC_SetPriority(HRTIM1_IRQn, 0, 0);  // 抢占优先级0
HAL_NVIC_SetPriority(TIM1_IRQn, 1, 0);   // 抢占优先级1
HAL_NVIC_SetPriority(UART1_IRQn, 5, 0);   // 抢占优先级5

// HRTIM可以打断TIM1和UART1
// TIM1可以打断UART1，但不能打断HRTIM
// UART1不能打断任何其他中断
```

### GROUP_2（推荐用于中等复杂度系统）
- 抢占优先级：2位（0-3，共4级）
- 子优先级：2位（0-3，共4级）

**特点：**
- ✅ 4个抢占优先级级别（通常足够）
- ✅ 每个抢占优先级内可以有4个不同的子优先级
- ✅ 更灵活，适合大多数应用

**示例：**
```c
HAL_NVIC_SetPriority(HRTIM1_IRQn, 0, 0);  // 抢占0，子0
HAL_NVIC_SetPriority(TIM1_IRQn, 0, 1);    // 抢占0，子1
HAL_NVIC_SetPriority(UART1_IRQn, 1, 0);   // 抢占1，子0

// HRTIM和TIM1抢占优先级相同，不能相互打断
// 但如果同时发生，HRTIM先执行（子优先级0 < 1）
// UART1可以打断HRTIM和TIM1（抢占优先级1 < 0）
```

## 优先级判断规则

中断优先级判断顺序（从高到低）：
1. **抢占优先级**（数值越小越高）
2. **子优先级**（数值越小越高）
3. **硬件优先级**（IRQ号越小越高）

### 示例对比

假设使用 GROUP_2，有以下中断同时发生：

| 中断 | 抢占优先级 | 子优先级 | IRQ号 | 执行顺序 |
|------|-----------|---------|-------|---------|
| HRTIM | 0 | 0 | 29 | **1**（抢占优先级最高） |
| TIM1 | 0 | 1 | 25 | **2**（抢占相同，子优先级更高） |
| UART1 | 1 | 0 | 37 | **3**（抢占优先级较低） |
| DMA1 | 1 | 2 | 11 | **4**（抢占相同，子优先级更高） |

## 实际应用建议

### 你的项目（编码器触发 + PWM控制）

**推荐配置：GROUP_2**

```c
// 1. 设置优先级分组
HAL_NVIC_SetPriorityGrouping(NVIC_PRIORITYGROUP_2);

// 2. 关键实时任务 - 抢占优先级0
HAL_NVIC_SetPriority(HRTIM1_Master_IRQn, 0, 0);  // PWM生成，最高优先级
HAL_NVIC_SetPriority(HRTIM1_TIMB_IRQn, 0, 1);    // PWM相关，次高
HAL_NVIC_SetPriority(TIM1_UP_TIM16_IRQn, 0, 2);   // 编码器触发定时器

// 3. 数据采集 - 抢占优先级1
HAL_NVIC_SetPriority(DMA1_Channel1_IRQn, 1, 0);   // ADC1 DMA
HAL_NVIC_SetPriority(DMA1_Channel2_IRQn, 1, 1);   // ADC2 DMA

// 4. 串口发送 - 抢占优先级2
HAL_NVIC_SetPriority(DMA2_Channel2_IRQn, 2, 0);   // UART3_TX
HAL_NVIC_SetPriority(DMA2_Channel3_IRQn, 2, 1);   // UART5_TX
HAL_NVIC_SetPriority(DMA2_Channel4_IRQn, 2, 2);   // UART4_TX

// 5. 串口接收 - 抢占优先级3（最低）
HAL_NVIC_SetPriority(USART1_IRQn, 3, 0);
HAL_NVIC_SetPriority(USART2_IRQn, 3, 1);
HAL_NVIC_SetPriority(USART3_IRQn, 3, 2);
HAL_NVIC_SetPriority(UART4_IRQn, 3, 2);
HAL_NVIC_SetPriority(UART5_IRQn, 3, 3);
```

**执行效果：**
- ✅ HRTIM中断可以打断所有其他中断（抢占优先级0最高）
- ✅ TIM1中断可以打断数据采集和串口中断
- ✅ 如果多个UART发送DMA同时完成，按子优先级顺序处理
- ✅ 串口接收不会影响实时任务

## 总结

### 抢占优先级
- **作用**：控制中断嵌套（打断）
- **规则**：高抢占优先级可以打断低抢占优先级
- **类比**：医院的急诊科优先级

### 子优先级
- **作用**：控制相同抢占优先级中断的排队顺序
- **规则**：不能用于中断嵌套，只能决定排队顺序
- **类比**：银行VIP客户之间的排队顺序

### 选择建议
- **GROUP_4**：需要精细控制16个不同优先级级别
- **GROUP_2**：大多数应用的最佳选择，平衡灵活性和复杂度
- **GROUP_0**：不使用中断嵌套，仅顺序执行
